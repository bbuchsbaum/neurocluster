.Rproj.user
.Rhistory
.RData
.Ruserdata
_rlib/
src/*.o
src/*.so
src/*.dll
neurocluster_*.tar.gz

# Local tooling artifacts
.omc/
.mote/
.rcmdcheck_result.rds
..Rcheck/

# Benchmark outputs (keep inst/benchmarks/results.csv tracked)
inst/benchmarks/*.log
inst/benchmarks/*_raw.csv
inst/benchmarks/*_summary.csv
inst/benchmarks/*_best_by_family.csv
inst/benchmarks/*_ab*.csv

# Editor / OS junk
.DS_Store
**/.DS_Store

# Local Claude settings (personal, not shared)
.claude/settings.local.json

# Locally installed copy of the package used for dev checks
.Rlib/

# albersdown vignette backups
.albersdown.bak/

# Archived one-off notes, patches, and verification scripts kept out of the
# package root. Retained locally; history has them if they are ever needed.
archive/
