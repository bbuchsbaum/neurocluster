library(neurocluster)
library(neuroim2)
library(neighborweights)

# Check the function signature
print("Function signature for weighted_spatial_adjacency:")
print(args(neighborweights::weighted_spatial_adjacency))

# Also try help
# help(weighted_spatial_adjacency, package="neighborweights")