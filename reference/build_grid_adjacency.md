# Internal: build 3D grid adjacency for masked voxels (fast C++ version)

Internal: build 3D grid adjacency for masked voxels (fast C++ version)

## Usage

``` r
build_grid_adjacency(mask, mask_idx, connectivity)
```
