# Grid-based seed initialization

Places seeds on a regular grid within the mask.

## Usage

``` r
init_grid_seeds(coords, n_clusters)
```

## Arguments

- coords:

  Coordinate matrix

- n_clusters:

  Number of clusters

## Value

List with seeds, initial_labels, and seed_coords
