# Common Utilities for 4D Clustering Algorithms

Internal functions shared across all cluster4d methods for consistency
and code reuse.

## Usage

``` r
validate_cluster4d_inputs(vec, mask, n_clusters, method = "cluster4d")
```
