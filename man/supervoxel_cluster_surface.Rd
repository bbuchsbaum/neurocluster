% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supervoxels.R
\name{supervoxel_cluster_surface}
\alias{supervoxel_cluster_surface}
\title{Supervoxel Clustering on a Surface}
\usage{
supervoxel_cluster_surface(
  bsurf,
  K = 500,
  sigma1 = 1,
  sigma2 = 5,
  iterations = 50,
  connectivity = 6,
  use_medoid = FALSE
)
}
\arguments{
\item{bsurf}{A \code{NeuroSurface} or similar object with geometry, coords, and data.}

\item{K}{Number of clusters.}

\item{sigma1}{Heat kernel bandwidth for feature similarity (data vectors).}

\item{sigma2}{Heat kernel bandwidth for spatial similarity (coordinate vectors).}

\item{iterations}{Max iterations.}

\item{connectivity}{Neighborhood size on the surface (e.g., # of nearest mesh neighbors).}

\item{use_medoid}{Whether to use medoids for cluster centers.}
}
\value{
A \code{list} with:
\describe{
  \item{clusvol}{A \code{NeuroSurface} storing the final clustering result.}
  \item{clusters}{Integer vector of cluster assignments (one per vertex).}
  \item{centers}{Matrix of cluster centers.}
  \item{coord_centers}{Matrix of spatial centroid coordinates.}
  \item{index_sets}{List of vertex indices for each cluster.}
}
}
\description{
Cluster feature data on a cortical surface or mesh using a supervoxel-like approach.
}
