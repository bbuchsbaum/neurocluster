% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_volume.R
\name{generate_synthetic_volume}
\alias{generate_synthetic_volume}
\title{Generate a synthetic 4D neuroimaging volume with labeled clusters}
\usage{
generate_synthetic_volume(
  scenario = c("gaussian_blobs", "z_layers", "checkerboard"),
  dims = c(16, 16, 8),
  n_clusters = 4,
  n_time = 30,
  spread = c(3.5, 3.5, 2),
  noise_sd = 0.05,
  amplitude_range = c(0.8, 1.2),
  spacing_mm = c(3, 3, 3),
  seed = NULL
)
}
\arguments{
\item{scenario}{Pattern to embed. One of \code{"gaussian_blobs"}, \code{"z_layers"},
or \code{"checkerboard"}.}

\item{dims}{Integer vector of length 3 giving the spatial grid dimensions.}

\item{n_clusters}{Number of latent clusters to embed.}

\item{n_time}{Number of time points.}

\item{spread}{Characteristic spread of each Gaussian blob (scalar or length 3).}

\item{noise_sd}{Standard deviation of additive Gaussian noise.}

\item{amplitude_range}{Range used when scaling each voxel's amplitude.}

\item{spacing_mm}{Physical voxel size supplied to the generated \code{NeuroSpace}.}

\item{seed}{Optional seed for reproducibility.}
}
\value{
A list with elements \code{vec}, \code{mask}, \code{truth}, \code{coords},
\code{patterns}, \code{weights}, \code{dims}, \code{n_clusters}, and \code{scenario}.
}
\description{
Convenient synthetic data for examples, vignettes, and tests. Each scenario
produces a \code{NeuroVec} (time series volume), a \code{NeuroVol} mask, and the
ground-truth cluster labels so you can benchmark algorithms or build
illustrations quickly.
}
\examples{
syn <- generate_synthetic_volume(
  scenario = "gaussian_blobs",
  dims = c(12, 12, 6),
  n_clusters = 4,
  seed = 1
)
str(syn)
truth_vol <- array(syn$truth, syn$dims)
clusvol <- neuroim2::ClusteredNeuroVol(syn$mask > 0, clusters = syn$truth)
plot(clusvol, slice = c(6, 6, 3), view = "axial")
}
