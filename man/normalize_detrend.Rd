% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{normalize_detrend}
\alias{normalize_detrend}
\title{Normalize and Detrend Neuroimaging Data}
\usage{
normalize_detrend(vec, mask = NULL)
}
\arguments{
\item{vec}{A \code{NeuroVec} object (4D neuroimaging data)}

\item{mask}{Optional \code{NeuroVol} mask. If NULL, uses all non-zero voxels.}
}
\value{
A \code{NeuroVec} with volume offsets and linear trends removed
}
\description{
Combined fast preprocessing: removes volume mean offsets and linear
trends in a single efficient operation.
}
\details{
Combines \code{\link{normalize_volumes}} and \code{\link{detrend_time}}
in a single pass for efficiency. The operations are applied in order:
\enumerate{
\item Remove volume-wise mean offset
\item Remove linear trend from each voxel's timeseries
}

Uses fast C++ parallel implementation via RcppParallel.
}
\examples{
\dontrun{
vec <- neuroim2::read_vec("functional.nii.gz")
vec_clean <- normalize_detrend(vec)
}

}
