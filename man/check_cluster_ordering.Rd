% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_utils.R
\name{check_cluster_ordering}
\alias{check_cluster_ordering}
\title{Check for Spatial Ordering in Cluster IDs}
\usage{
check_cluster_ordering(cluster_result)
}
\arguments{
\item{cluster_result}{A clustering result object with \code{coord_centers} component}
}
\value{
A list with correlation statistics:
\itemize{
\item \code{cor_x}: Spearman correlation between cluster ID and x-coordinate
\item \code{cor_y}: Spearman correlation between cluster ID and y-coordinate
\item \code{cor_z}: Spearman correlation between cluster ID and z-coordinate
\item \code{max_abs_cor}: Maximum absolute correlation across all axes
\item \code{is_ordered}: Logical indicating if any axis shows strong ordering (|r| > 0.3)
}
}
\description{
Diagnostic function to detect whether cluster IDs are spatially ordered
along any axis. Useful for identifying clustering algorithms that may
produce ordered IDs.
}
\examples{
\dontrun{
result <- cluster4d(vec, mask, K = 100, method = "flash3d")

# Check for ordering
ordering <- check_cluster_ordering(result)
print(ordering)

# If strongly ordered, randomize
if (ordering$is_ordered) {
  result <- randomize_cluster_ids(result)
}
}

}
