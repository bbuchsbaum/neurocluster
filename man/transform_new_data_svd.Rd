% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/g3s_compression.R
\name{transform_new_data_svd}
\alias{transform_new_data_svd}
\title{Transform New Data Using Existing SVD Compression}
\usage{
transform_new_data_svd(new_data, compression_result)
}
\arguments{
\item{new_data}{Numeric matrix (N_new x T) with same number of columns as
the original training data.}

\item{compression_result}{List returned by \code{\link{compress_features_svd}}
containing the rotation matrix and scaling parameters.}
}
\value{
Compressed and normalized feature matrix (N_new x M) where M is the
number of components in the original compression.
}
\description{
Applies a previously computed SVD compression to new data, ensuring consistency
across multiple runs or when adding new voxels.
}
\examples{
# Train on subset of data
train_data <- matrix(rnorm(500 * 300), 500, 300)
compression <- compress_features_svd(train_data, n_components = 15)

# Apply to new data
test_data <- matrix(rnorm(100 * 300), 100, 300)
compressed_test <- transform_new_data_svd(test_data, compression)

}
